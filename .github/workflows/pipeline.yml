name: Real Estate Pipeline

on:
  schedule:
    - cron: '0 5 * * *'   # every day at 02:00 UTC
  workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  # ==============================================================
  # 1. Links loader → Supabase
  # ==============================================================
  collect-links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install latest stable Chrome + auto-matched driver
        uses: browser-actions/setup-chrome@v2
        with:
          chrome-version: stable

      - name: Clean & force correct undetected-chromedriver version
        run: |
          rm -rf ~/.local/share/undetected_chromedriver
          MAJOR=$(google-chrome --version | grep -oE '[0-9]+' | head -1)
          mkdir -p ~/.local/share/undetected_chromedriver
          echo "$MAJOR" > ~/.local/share/undetected_chromedriver/VERSION

      - name: Run links loader → Supabase
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: |
          python -m pipeline.acquisition.links_loader --start 1 --end 100
          python -m pipeline.acquisition.links_loader --start 101 --end 200

  # ==============================================================
  # 1. Links loader → Supabase
  # ==============================================================
  collect-links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install latest stable Chrome + auto-matched driver
        uses: browser-actions/setup-chrome@v2
        with:
          chrome-version: stable

      - name: Clean & force correct undetected-chromedriver version
        run: |
          rm -rf ~/.local/share/undetected_chromedriver
          MAJOR=$(google-chrome --version | grep -oE '[0-9]+' | head -1)
          mkdir -p ~/.local/share/undetected_chromedriver
          echo "$MAJOR" > ~/.local/share/undetected_chromedriver/VERSION

      - name: Run links loader → Supabase
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: |
          python -m pipeline.acquisition.links_loader --start 1 --end 100
          python -m pipeline.acquisition.links_loader --start 101 --end 200
  # ==============================================================
  # 2. Bronze-scraping → Supabase
  # ==============================================================
  parse-bronze:
    needs: collect-links
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install Chrome + force correct driver
        uses: browser-actions/setup-chrome@v2
        with:
          chrome-version: stable
      - run: |
          rm -rf ~/.local/share/undetected_chromedriver
          MAJOR=$(google-chrome --version | grep -oE '[0-9]+' | head -1)
          mkdir -p ~/.local/share/undetected_chromedriver
          echo "$MAJOR" > ~/.local/share/undetected_chromedriver/VERSION

      - name: Run bronze loader → Supabase
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: |
          python -m pipeline.bronze.loader --start 0 --end 1000
          python -m pipeline.bronze.loader --start 1001 --end 2000
          python -m pipeline.bronze.loader --start 2001 --end 3000
          python -m pipeline.bronze.loader --start 3001 --end 4000
          python -m pipeline.bronze.loader --start 4001 --end 5000
          python -m pipeline.bronze.loader --start 5001 --end 6000
          python -m pipeline.bronze.loader --start 6001 --end 7000
          python -m pipeline.bronze.loader --start 7001 --end 8000
          #python -m pipeline.bronze.loader --start 8001 --end 9000
         # python -m pipeline.bronze.loader --start 9001 --end 10000
         # python -m pipeline.bronze.loader --start 10001 --end 11000
         # python -m pipeline.bronze.loader --start 11001 --end 12000
          #python -m pipeline.bronze.loader --start 12001 --end 13000
          #python -m pipeline.bronze.loader --start 13001 --end 14000
          #python -m pipeline.bronze.loader --start 14001 --end 15000
          #python -m pipeline.bronze.loader --start 15001 --end 16000
          #python -m pipeline.bronze.loader --start 16001 --end 17000
  # ==============================================================
  # 3. Silver → Supabase
  # ==============================================================
  silver:
    needs: parse-bronze
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - name: Silver → Supabase
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: python -m scripts.run_silver

  # ==============================================================
  # 4. Gold → Supabase
  # ==============================================================
  gold:
    needs: silver
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - name: Gold → Supabase
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: python -m scripts.run_gold
